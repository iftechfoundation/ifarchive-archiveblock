<IfModule archiveblock_module>

    # This config file is provided to give an overview of the directives,
    # which are only allowed in the 'server config' context.
    # See https://github.com/iftechfoundation/ifarchive-archiveblock for
    # the full explanation.

    # ArchiveBlockMapPath /var/ifarchive/lib/blocktag.map
    # The path to the block map.

    # ArchiveBlockRestrictDomain ukrestrict.ifarchive.org
    # The domain which handles tagged files.
    
</IfModule>

